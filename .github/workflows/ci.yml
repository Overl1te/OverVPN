name: CI

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  API_IMAGE: ghcr.io/overl1te/overvpn-api
  WEB_IMAGE: ghcr.io/overl1te/overvpn-web

jobs:
  verify:
    runs-on: [self-hosted, linux, x64, overvpn]
    timeout-minutes: 30
    env:
      NODE_ENV: test
      WORKERS_ENABLED: 'false'
      DATABASE_URL: postgresql://overvpn:ci@127.0.0.1:5432/overvpn?schema=public
      REDIS_URL: redis://127.0.0.1:6379/0
      CORS_ORIGINS: http://localhost:5173
      SUB_PUBLIC_BASE_URL: http://localhost:5173
      JWT_ACCESS_SECRET: ci-jwt-access-secret-abcdefghijklmnopqrstuvwxyz0123456789
      SECRETS_MASTER_KEY: '0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef'
      SING_BOX_CLASH_API_SECRET: 'fedcba9876543210fedcba9876543210fedcba9876543210fedcba9876543210'
      BOOTSTRAP_ADMIN_USER: owner
      BOOTSTRAP_ADMIN_PASSWORD: bootstrap-password-16chars
      BACKUP_DIR: /tmp/overvpn-backups
      BACKUP_RETENTION_DAYS: '30'
      BACKUP_ENCRYPT: 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.12.0

      # Self-hosted: avoid actions/setup-node (slow ~280MB download + flaky GH cache timeouts).
      # Node is installed once into $HOME/.local/node and reused across jobs.
      - name: Ensure Node.js 24
        run: bash scripts/ci-ensure-node.sh

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma client
        run: pnpm prisma:generate

      - name: Format check
        run: pnpm format:check

      - name: Lint
        run: pnpm lint

      - name: Typecheck
        run: pnpm typecheck

      - name: Unit tests
        run: pnpm test

      - name: Build
        run: pnpm build

  publish:
    needs: verify
    if: >
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'push' &&
       (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')))
    runs-on: [self-hosted, linux, x64, overvpn]
    timeout-minutes: 90
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Ensure Docker access
        run: |
          if ! docker info >/dev/null 2>&1; then
            echo "::error::Docker socket is not accessible for $(whoami)."
            echo "Fix on the runner host:"
            echo "  sudo usermod -aG docker $(whoami)"
            echo "  sudo systemctl restart 'actions.runner.*.service'"
            echo "Then re-login / restart the runner service so the new group applies."
            exit 1
          fi
          docker version

      - name: Ensure buildx cache dirs
        run: |
          CACHE_ROOT="${HOME}/.cache/overvpn-buildx"
          mkdir -p "${CACHE_ROOT}/api" "${CACHE_ROOT}/web"
          echo "BUILDX_CACHE_ROOT=${CACHE_ROOT}" >> "$GITHUB_ENV"

      - name: Remove broken buildx builder
        run: |
          set +e
          docker buildx rm -f overvpn-ci 2>/dev/null || true
          docker ps -aq --filter "name=buildx_buildkit_overvpn-ci" | xargs -r docker rm -f || true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          name: overvpn-ci
          driver: docker-container
          cleanup: true
          # keepstorage is in MB (not "5GB") — invalid units crash buildkitd in a restart loop
          buildkitd-flags: --oci-worker-gc --oci-worker-gc-keepstorage=5000

      - name: Clean stale Docker resources
        run: |
          set +e
          docker buildx version
          docker buildx ls --format '{{.Name}}' | while read -r builder; do
            [[ -z "$builder" || "$builder" == "default" || "$builder" == "overvpn-ci" ]] && continue
            echo "Removing stale builder: ${builder}"
            docker buildx rm "$builder" 2>/dev/null || true
          done
          docker buildx prune -af 2>/dev/null || true
          df -h /

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata (API)
        id: meta-api
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.API_IMAGE }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,prefix=sha-,format=short
            type=ref,event=tag
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - name: Docker metadata (Web)
        id: meta-web
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.WEB_IMAGE }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,prefix=sha-,format=short
            type=ref,event=tag
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - name: Prefetch Xray release zip
        env:
          XRAY_VERSION: v26.6.27
        run: |
          set -euo pipefail
          mkdir -p .docker-cache/xray
          # Runner arch matches the image we publish today (linux/amd64).
          dest=".docker-cache/xray/Xray-linux-64.zip"
          if [[ -s "$dest" ]]; then
            echo "Using cached ${dest} ($(du -h "$dest" | cut -f1))"
            exit 0
          fi
          echo "Downloading Xray ${XRAY_VERSION} to ${dest}"
          curl -fsSL \
            --retry 8 \
            --retry-all-errors \
            --retry-delay 5 \
            --connect-timeout 30 \
            --max-time 600 \
            -o "${dest}.partial" \
            "https://github.com/XTLS/Xray-core/releases/download/${XRAY_VERSION}/Xray-linux-64.zip"
          mv -f "${dest}.partial" "$dest"
          ls -lh "$dest"

      - name: Build and push API (+ sing-box)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/api/Dockerfile
          push: true
          tags: ${{ steps.meta-api.outputs.tags }}
          labels: ${{ steps.meta-api.outputs.labels }}
          build-args: |
            OVERVPN_GIT_SHA=${{ github.sha }}
          cache-from: type=local,src=${{ env.BUILDX_CACHE_ROOT }}/api
          cache-to: type=local,dest=${{ env.BUILDX_CACHE_ROOT }}/api,mode=min

      - name: Build and push Web
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/web/Dockerfile
          push: true
          tags: ${{ steps.meta-web.outputs.tags }}
          labels: ${{ steps.meta-web.outputs.labels }}
          cache-from: type=local,src=${{ env.BUILDX_CACHE_ROOT }}/web
          cache-to: type=local,dest=${{ env.BUILDX_CACHE_ROOT }}/web,mode=min

      - name: Package deploy bundle
        run: bash scripts/package-deploy-bundle.sh dist/overvpn-deploy.tar.gz

      - name: Upload deploy bundle artifact
        uses: actions/upload-artifact@v4
        with:
          name: overvpn-deploy
          path: dist/overvpn-deploy.tar.gz
          if-no-files-found: error

      - name: Clean up Docker build resources
        if: always()
        run: |
          set +e
          docker buildx prune -af 2>/dev/null || true
          docker buildx ls --format '{{.Name}}' | while read -r builder; do
            [[ -z "$builder" || "$builder" == "default" || "$builder" == "overvpn-ci" ]] && continue
            echo "Removing leftover builder: ${builder}"
            docker buildx rm "$builder" 2>/dev/null || true
          done
          echo "Disk after cleanup:"
          df -h /
          docker system df
